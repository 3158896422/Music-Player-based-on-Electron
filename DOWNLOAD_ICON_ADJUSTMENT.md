# 下载图标显示调整

## 调整说明

根据用户要求，已调整下载图标的显示策略：

### 调整前
- 本地歌曲 - 显示勾勾图标 (✓)
- 已下载的在线音乐 - 显示勾勾图标 (✓)
- 未下载的在线音乐 - 显示下载图标 (⬇️)

### 调整后
- **本地歌曲** - **不显示任何图标**
- **已下载的在线音乐** - **不显示任何图标**
- **未下载的在线音乐** - **显示下载图标 (⬇️)**

## 修改内容

### 1. UI 模板调整

在所有音乐列表视图中，添加条件判断 `v-if="!song.path && !song.downloaded"`：

```html
<button class="download-btn" @click.stop="handleDownload(song)" :title="getDownloadTitle(song)" v-if="!song.path && !song.downloaded">
  <svg><use :href="getDownloadIcon(song)"/></svg>
</button>
```

这样只有未下载的在线音乐才会显示下载按钮。

### 2. 辅助方法简化

**`getDownloadIcon(song)`** - 始终返回下载图标：
```javascript
const getDownloadIcon = (song) => {
  // 只显示下载图标
  return '#icon-download';
};
```

**`getDownloadTitle(song)`** - 始终返回"下载"：
```javascript
const getDownloadTitle = (song) => {
  return '下载';
};
```

## 修改的文件

1. **`index.html`**
   - 专辑详情视图 - 添加 `v-if` 条件
   - 艺术家详情视图 - 添加 `v-if` 条件
   - 歌单详情视图 - 添加 `v-if` 条件
   - 其他视图 (我喜欢、最近播放、库) - 添加 `v-if` 条件
   - 播放列表面板 - 添加 `v-if` 条件

2. **`app.js`**
   - 简化 `getDownloadIcon` 方法
   - 简化 `getDownloadTitle` 方法

## 预期效果

现在在所有视图中：

- ✅ **本地歌曲** - 不显示下载按钮，界面更简洁
- ✅ **已下载的在线音乐** - 不显示下载按钮，避免冗余
- ✅ **未下载的在线音乐** - 显示下载图标 (⬇️)，点击可下载

## 功能保持

虽然本地歌曲和已下载歌曲不再显示图标，但功能仍然保留：

1. **已下载检测** - 系统仍会自动检测已下载的在线音乐
2. **优先播放本地** - 播放已下载歌曲时仍会使用本地文件
3. **下载功能** - 点击未下载歌曲的下载图标仍可下载

## 优势

1. **界面更简洁** - 减少视觉干扰
2. **重点突出** - 只显示需要的操作（下载）
3. **符合直觉** - 已下载的不需要再显示操作按钮

## 版本信息

- **调整日期**: 2026-03-24
- **版本**: v3.1.2
- **状态**: ✅ 已调整
