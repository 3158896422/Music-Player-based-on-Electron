# ⚠️ 重要：打包操作说明

## 问题原因

由于 **Trae IDE 的沙箱安全限制**，在 IDE 内置终端中运行 electron-builder 打包时，无法访问 Windows 系统的缓存目录：
```
C:\Users\Lenovo\AppData\Local\electron-builder\Cache\winCodeSign
```

这导致解压代码签名工具时失败（无法创建符号链接）。

## ✅ 解决方案（必须执行）

### 步骤 1：关闭 Trae IDE（可选但推荐）

为了避免沙箱限制，建议在**外部 PowerShell**中完成打包。

### 步骤 2：打开外部 PowerShell

1. 按 `Win + R`
2. 输入 `powershell`
3. 按回车

### 步骤 3：执行打包命令

在 PowerShell 中依次执行以下命令：

```powershell
# 进入项目目录
cd "d:\Trae CN\project\Music_Player\MusicPlayer-Electron"

# 设置环境变量（禁用代码签名）
$env:CSC_IDENTITY_AUTO_DISCOVERY="false"

# 开始打包
npm run build:win
```

或者使用一行命令：

```powershell
cd "d:\Trae CN\project\Music_Player\MusicPlayer-Electron"; $env:CSC_IDENTITY_AUTO_DISCOVERY="false"; npm run build:win
```

### 步骤 4：等待打包完成

打包过程需要 **5-10 分钟**，请耐心等待。你会看到以下进度：

1. ✓ loading configuration
2. ✓ executing @electron/rebuild
3. ✓ installing native dependencies
4. ✓ packaging platform=win32
5. ✓ building target=nsis
6. ✓ finished building

### 步骤 5：查找安装包

打包完成后，安装包位于：

```
d:\Trae CN\project\Music_Player\MusicPlayer-Electron\dist\Music Player Setup 1.0.0.exe
```

## 🚀 快速打包脚本

我已经创建了 `build.bat` 文件，你也可以：

1. 打开文件资源管理器
2. 导航到 `d:\Trae CN\project\Music_Player\MusicPlayer-Electron\`
3. 双击运行 `build.bat`

## 📦 打包配置说明

当前配置会生成：

- **安装包类型**: NSIS 安装程序（.exe）
- **架构**: x64（64 位 Windows）
- **安装选项**:
  - ✅ 允许用户选择安装目录
  - ✅ 创建桌面快捷方式
  - ✅ 创建开始菜单快捷方式
- **输出目录**: `dist/`

## 🔧 故障排除

### 问题 1：npm 命令不存在

**解决方法**：
```powershell
# 检查 Node.js 是否安装
node -v

# 如果没有安装，请访问 https://nodejs.org/ 下载安装
```

### 问题 2：权限错误

**解决方法**：
1. 右键点击 PowerShell 图标
2. 选择"以管理员身份运行"
3. 重新执行打包命令

### 问题 3：网络错误（下载失败）

**解决方法**：
```powershell
# 使用国内镜像源
npm config set registry https://registry.npmmirror.com
npm config set electron_mirror https://npmmirror.com/mirrors/electron/

# 清理缓存
npm cache clean --force

# 重新打包
```

### 问题 4：打包后程序无法启动

**解决方法**：
1. 检查 Windows Defender 或其他杀毒软件是否拦截
2. 尝试运行未打包版本测试：
   ```powershell
   npm start
   ```

## 📝 验证打包结果

打包成功后，`dist` 目录中应该包含：

```
dist/
├── win-unpacked/          # 未打包的便携版本（测试用）
├── builder-effective-config.yaml  # 打包配置
└── Music Player Setup 1.0.0.exe   # ⭐ 安装包（分发给用户）
```

## 💡 提示

- 首次打包时间较长，因为需要下载 Electron 运行时
- 后续打包会快很多，因为缓存已建立
- 安装包大小约为 150-200 MB
- 用户可以像安装其他 Windows 软件一样双击安装包安装

## 🎯 下一步

打包完成后，你可以：

1. 在另一台 Windows 电脑上测试安装包
2. 将安装包分发给用户
3. 创建软件发布说明

---

**需要帮助？** 查看 `BUILD_GUIDE.md` 获取更详细的指南。
