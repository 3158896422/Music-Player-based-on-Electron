# 在线音乐下载功能实现

## 功能概述

本次更新为音乐播放器添加了完整的在线音乐下载功能，包括:
1. 在我喜欢、自建歌单和最近播放的在线音乐后面添加下载图标
2. 已下载的歌曲显示勾勾图标 (✓)
3. 优先播放已下载的本地音频文件

## 实现的功能

### 1. 下载图标显示

**位置**: 在我喜欢、自建歌单、最近播放和所有在线音乐列表中的每首歌曲后面

**图标状态**:
- ⬇️ **下载图标**: 未下载的在线音乐显示下载箭头图标
- ✓ **勾勾图标**: 已下载的在线音乐或本地歌曲显示勾勾图标

**实现文件**:
- `index.html` - 在所有音乐列表视图中添加下载按钮
- `app.js` - 添加 `handleDownload` 方法处理下载逻辑

### 2. 下载状态检测

**功能**: 自动检测在线音乐是否已下载到本地

**实现方式**:
- 使用 `isSongDownloaded` 方法检查下载目录中是否存在歌曲文件
- 根据歌曲名和艺术家名构建文件名，检查各种音频格式 (.mp3, .flac, .wav, .m4a, .ogg)
- 使用缓存机制避免重复检查，提高性能

**实现文件**:
- `app.js` - `isSongDownloaded` 方法
- `main.js` - `get-download-path` IPC 处理程序

### 3. 优先播放本地音频

**功能**: 当在线音乐已下载时，优先播放本地文件而不是在线播放

**实现逻辑**:
1. 播放歌曲时，首先检查是否已下载
2. 如果已下载，直接使用本地文件播放
3. 如果未下载，才获取在线 URL 进行播放

**实现文件**:
- `app.js` - `playSourceSong` 方法修改
- `app.js` - `playLocalFile` 新方法

### 4. 下载目录管理

**下载路径**: 用户的音乐文件夹下的 `MusicPlayer_Downloads` 目录
- Windows: `C:\Users\{用户名}\Music\MusicPlayer_Downloads`

**实现文件**:
- `main.js` - `get-download-path` IPC 处理程序

## 修改的文件

### 1. `app.js`

**新增方法**:
- `isSongDownloaded(song)` - 检查歌曲是否已下载
- `handleDownload(song)` - 处理下载按钮点击
- `playLocalFile(filePath, songInfo)` - 播放本地文件

**修改方法**:
- `playSourceSong(song)` - 添加优先播放本地文件的逻辑

**新增变量**:
- `downloadStatusCache` - 下载状态缓存 Map

**导出**:
- 添加 `handleDownload` 到导出对象

### 2. `index.html`

**新增图标**:
- `icon-check` - 勾勾图标，用于表示已下载

**修改视图**:
- 专辑详情视图 - 添加下载按钮
- 艺术家详情视图 - 添加下载按钮
- 歌单详情视图 - 添加下载按钮
- 其他视图 (我喜欢、最近播放、库) - 添加下载按钮
- 播放列表面板 - 添加下载按钮

### 3. `main.js`

**新增 IPC 处理程序**:
- `get-download-path` - 获取下载目录路径

**修改**:
- `download-file` - 使用下载目录作为默认保存路径

## 使用方法

### 下载在线音乐

1. 在任意在线音乐列表中，找到想要下载的歌曲
2. 点击歌曲后面的下载图标 (⬇️)
3. 选择音质 (128k/320k/FLAC)
4. 选择保存位置 (默认为 MusicPlayer_Downloads 目录)
5. 等待下载完成

### 播放已下载的音乐

1. 已下载的歌曲会显示勾勾图标 (✓)
2. 点击歌曲直接播放，会自动使用本地文件播放
3. 或者点击勾勾图标，也会播放本地文件

### 自动优先播放本地文件

- 当播放已下载的在线音乐时，系统会自动检测并使用本地文件
- 无需手动操作，享受更快速、稳定的播放体验

## 技术细节

### 下载状态缓存

使用 `Map` 对象缓存下载状态，避免重复检查:
```javascript
const downloadStatusCache = new Map();
```

缓存键：`song.hash || song.id || song.songmid || song.url`
缓存值：`{ downloaded: boolean, path?: string }`

### 文件匹配逻辑

检查下载状态时，会尝试以下文件名格式:
- `{歌曲名} - {艺术家名}.mp3`
- `{歌曲名} - {艺术家名}.flac`
- `{歌曲名} - {艺术家名}.wav`
- `{歌曲名} - {艺术家名}.m4a`
- `{歌曲名} - {艺术家名}.ogg`

### 播放优先级

1. 本地歌曲 (song.path 存在) - 直接播放
2. 已下载的在线音乐 - 播放本地文件
3. 未下载的在线音乐 - 获取在线 URL 播放

## 注意事项

1. **下载目录**: 建议不要手动修改下载目录，以免影响下载状态检测
2. **文件命名**: 下载的文件会自动嵌入元数据 (封面、歌词、艺术家信息等)
3. **缓存清理**: 下载状态缓存会在应用重启后清空，下次会重新检测
4. **网络依赖**: 未下载的在线音乐需要网络连接才能播放

## 未来改进

1. 添加下载管理界面，显示下载进度和历史
2. 支持批量下载
3. 支持自定义下载目录
4. 添加下载音质设置
5. 支持离线模式，只显示已下载的歌曲

## 测试建议

1. 测试下载在线音乐并检查是否正确显示勾勾图标
2. 测试播放已下载的音乐是否使用本地文件
3. 测试在不同视图 (我喜欢、最近播放、歌单) 中下载功能是否正常
4. 测试下载状态缓存是否正常工作
5. 测试不同音质的下载和播放

## 版本信息

- **实现日期**: 2026-03-24
- **版本**: v3.1.0
- **状态**: ✅ 已完成并测试
